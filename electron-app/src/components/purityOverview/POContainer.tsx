import POCDetails from "./POCDetails";

export default function POContainer(props: object) {

  return (
      <div className="po-container">
          POContainer
          <POCDetails/>
          <POCDetails/>
          <POCDetails/>
      </div>

  )
}